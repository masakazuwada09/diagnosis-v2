const LoadingScreen = () => {
	return (
		<div className="absolute top-0 left-0 w-full h-full bg-white backdrop-blur flex items-center justify-center bg-opacity-50">
			Loading...
		</div>
	);
};

export default LoadingScreen;
